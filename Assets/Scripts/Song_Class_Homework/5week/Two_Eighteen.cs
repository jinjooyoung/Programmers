using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Two_Eighteen : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //문자와 관련된 복합 대입 연산자
        string output = "hello";
        output += "world";
        output += "!!";
        Debug.Log(output);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
